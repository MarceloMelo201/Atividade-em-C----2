#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(){
	
	int i=1, numero;
		printf("Digite um valor: ");
		scanf("%d", &numero);
	
	for (i=1; i<=10; i++){
		
		
		printf("%d x %d = %d\n", numero, i, numero*i);
	}
